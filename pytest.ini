[pytest]
testpaths = tests/unit tests/integration tests/regression tests/e2e
markers =
    unit: 单元测试 — 无依赖、毫秒级
    integration: 集成测试 — 不依赖真网，可能用临时目录、mock
    regression: 回归测试 — 防止已修 bug 复发
    e2e: 端到端测试 — 调真实 MinerU 服务，需要有效 token，默认跳过
addopts = --strict-markers
asyncio_mode = auto
